# Versions pinned. Upgrade deliberately, not on every rebuild.

# Core
psycopg2-binary==2.9.9
sqlalchemy==2.0.30
pandas==2.2.2
numpy==1.26.4
pyyaml==6.0.1
python-dotenv==1.0.1

# Data
alpaca-py==0.30.1
# yfinance 0.2.x stops working when Yahoo rotates their backend (happens every
# few months). 1.3.0 is the current stable as of 2026-05; bump deliberately.
yfinance==1.3.0

# Backtesting + stats (chunks 3–4)
vectorbt==0.26.2
scipy==1.13.1
statsmodels==0.14.2

# Phase 8: meta-classifier (RandomForest on entry features)
scikit-learn==1.5.0

# Live + utilities (chunk 5)
requests==2.32.3
schedule==1.2.2
tenacity==8.4.1

# Research (chunk 3)
jupyterlab==4.2.2
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# Admin dashboard (chunk 7+)
streamlit==1.39.0
